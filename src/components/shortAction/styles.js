import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    actionItem: {
        gap: 4,
        padding: 12,
        alignItems: 'center',
    },
    textAction: {
        color: '#fff',
    },
});

export default styles;
