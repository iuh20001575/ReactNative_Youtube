import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    flex1: {
        flex: 1,
    },
    history: {
        height: 48,
        flexDirection: 'row',
        alignItems: 'center',
        gap: 12,
        paddingRight: 12,
    },
    allBtn: { color: '#0961D4' },
});

export default styles;
