import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    comment: {
        alignItems: 'center',
        gap: 7,
        flexDirection: 'row',
        flex: 1,
    },
    avatar: {
        width: 24,
        height: 24,
        borderRadius: 12,
    },
});

export default styles;
