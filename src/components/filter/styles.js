import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        gap: 8,
        paddingRight: 12,
    },
    filterItem: {
        borderRadius: 8,
        padding: 1,
    },
    filterItemBody: {
        borderRadius: 8,
        flexShrink: 0,
        flexGrow: 0,
    },
    textActive: {
        color: '#fff',
    },
});

export default styles;
