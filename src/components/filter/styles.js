import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        gap: 8,
    },
    filterItem: {
        borderRadius: 8,
        padding: 1,
    },
    filterItemBody: {
        borderRadius: 8,
    },
    textActive: {
        color: '#fff',
    },
});

export default styles;
