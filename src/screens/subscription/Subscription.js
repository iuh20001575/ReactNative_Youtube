import React from 'react';
import DefaultLayout from '~/layouts/defaultLayout/DefaultLayout';
import TextCustomize from '~/components/text';

const Subscription = () => {
    return (
        <DefaultLayout>
            <TextCustomize>Subcription page</TextCustomize>
        </DefaultLayout>
    );
};

export default Subscription;
