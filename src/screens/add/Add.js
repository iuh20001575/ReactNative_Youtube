import React from 'react';
import DefaultLayout from '~/layouts/defaultLayout';
import TextCustomize from '~/components/text';

const Add = () => {
    return (
        <DefaultLayout>
            <TextCustomize>Add page</TextCustomize>
        </DefaultLayout>
    );
};

export default Add;
